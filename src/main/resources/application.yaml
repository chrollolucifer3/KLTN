server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/learning_forum?createDatabaseIfNotExist=true"
    username: root
    password: cuong97
    driver-class-name: com.mysql.cj.jdbc.Driver
  task:
    scheduling:
      pool:
        size: 5
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  web:
    resources:
      static-locations: file:uploads/

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

jwt:
  secret: "7M/T%}Om]:Uq'Qi8cqG_k28R:>nvD,lR-]Ug6P*EXI9~$-y-Vh(w?oL]:2(m"
  expiration: 3
  refreshable-duration: 3

file:
  upload-dir: uploads/Image
